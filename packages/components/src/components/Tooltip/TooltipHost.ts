import { TooltipHostBase } from './TooltipHost.base'
import { getStyles } from './TooltipHost.styles'
import { styled } from '@/components/styled'

export const TooltipHost = styled(
  TooltipHostBase,
  getStyles,
)
